import { reviewerinfo } from 'osf-meetings/helpers/reviewerinfo';
import { module, test } from 'qunit';

module('Unit | Helper | reviewerinfo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewerinfo([42]);
  assert.ok(result);
});

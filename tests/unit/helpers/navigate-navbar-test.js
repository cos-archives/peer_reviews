import { navigateNavbar } from 'osf-meetings/helpers/navigate-navbar';
import { module, test } from 'qunit';

module('Unit | Helper | navigate navbar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = navigateNavbar([42]);
  assert.ok(result);
});
